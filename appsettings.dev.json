{
    "DatabaseConfig": {
        "ServerFilePath*": "secret/db-server",
        "NameFilePath*": "secret/db-name",
        "UserFilePath*": "secret/db-user",
        "PasswordFilePath*": "secret/db-password"
    }
}
